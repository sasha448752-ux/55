# Проверка цены: включена 15 сентября 2026

Проект: zjmldmkerretnrircslo. Серверная миграция: validate_new_canvas_price.

Добавлены только private.validate_new_canvas_price() и BEFORE INSERT триггер public.orders. Функция SECURITY INVOKER, search_path пустой; прямой вызов от PUBLIC, anon и authenticated отозван. RLS, старые строки и UPDATE не изменялись. Проверяет новые заказы по фиксированному прайсу 30 размеров; при изменении прайса нужна согласованная серверная правка.

Проверено:

- Временная таблица: правильная цена принимается; неправильная/NULL цена и неизвестный размер отклоняются; старые суммы и обновление статуса сохраняются.
- Рабочая таблица, роль anon: 20 × 20 см за 99000 коп. проходит, за 1 коп. отклоняется CANVAS_PRICE_CHANGED. Вся транзакция отменена ROLLBACK, тестовые заказы не сохранены.
- Проверка безопасности не выдала замечаний к новой функции. Отдельное предупреждение: отключена проверка скомпрометированных паролей: https://supabase.com/docs/guides/auth/password-security#password-strength-and-leaked-password-protection . Настройки Auth не изменены.

Откат: rollback-price-guard.sql. Отключает только эту проверку, не удаляет заказы. Полный файловый архив не является резервной копией базы.

Локальные изменения интерфейса ещё не опубликованы. Идемпотентность, подтверждение принадлежности загруженного файла и атомарное оформление корзины остаются отдельными задачами. Эта проверка не заменяет их.
